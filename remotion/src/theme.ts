// 공용 스타일 상수. 씬 컴포넌트 전반에서 재사용해 렌더 결과의 타이포그래피를 통일한다.
//
// Chromium(Remotion 렌더러)의 기본 UA 스타일은 라틴 문자에 세리프 계열을 쓰고 한글 글리프는
// 자동으로 산세리프 폰트로 폴백한다 — fontFamily를 명시하지 않으면 같은 문장 안에서 한글/영문
// 폰트가 섞여 보이는 원인이 된다. 렌더는 로컬 Windows 환경을 전제하므로(1차 범위) 웹폰트 번들링
// 없이 시스템 폰트 스택으로 라틴/한글 모두 산세리프로 통일한다.
export const FONT_FAMILY = "'Malgun Gothic', 'Segoe UI', sans-serif";
